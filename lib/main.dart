import 'package:flutter/material.dart';
import 'app.dart';

void main() {
  runApp(const FarrierLogApp());
}
// This is the main entry point of the Flutter application.
// It initializes the app by calling `runApp` with the `FarrierLogApp`